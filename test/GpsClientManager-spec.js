var GpsClientManager = require("../lib/GpsClientManager");
var gpsKey = "xxxxxxxxxxxxx";
var secret = "";

describe("greeter", function () {
  it("should greet with message", function () {
    //var client = new GpsClientManager(gpsKey,secret);

  });
});
